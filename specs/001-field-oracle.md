# spec 001: field oracle no. 01

status: accepted

accepted by the repository owner on 2026-09-18. implementation authorization
followed in the same project thread.

## transition

move the initial public repository from a readme-only shell to a client-only
gematria cootie catcher published as a github pages project site.

## included

- `AQ`, `Ordinal`, `QWER`, and `nQWER` as paired outer flaps;
- deterministic paper-plex v1 routing;
- thoth-adjacent major arcana names without reproduced artwork;
- return parity as upright/reversed bearing;
- eight authored field-01 fortunes;
- accessible animated and text receipts;
- one printable svg generated from the same edition data;
- tests, build, documentation, and github pages workflow;
- no backend, persistence, database, analytics, ai, or hidden randomness.

## provenance correction

the exploratory chat phrase `888 archaic aq` named a value/cipher context; it
was not evidence that the literal word `archaic` equals 888 in ordinary `AQ`.
the implementation preserves `888` as a paper-plex route vector and uses
engine-backed cipher vectors for phrase calculation.

## acceptance

- all four cipher mappings match canonical golden vectors;
- `888` routes to gate 8, the fool, fifth return, reversed;
- all eight gates and twenty-two arcana are reachable;
- repeated consultation is deterministic;
- unsupported and zero-valued offerings fail visibly;
- the browser, receipt, and print sheet share one domain/content source;
- keyboard and reduced-motion paths remain usable;
- production assets resolve beneath `/cootie-oracle/`;
- the static app sends no offering or runtime request to a third party;
- automated checks, rendered checks, and a physical fold boundary are reported
  separately rather than conflated.

## change shape

| dimension | changed | source of truth | proof |
|---|---:|---|---|
| user-visible behavior | yes | browser application | rendered desktop/mobile checks |
| persisted state | no | privacy contract | source and storage inspection |
| provider-visible behavior | yes | pages workflow | workflow and live-site smoke |
| identifiers, secrets, privacy | yes | privacy contract | no env/runtime network dependency |
| lifecycle states | yes | ui state machine | unit and interaction checks |
| setup and recovery | yes | development guide | cold-start commands |
| normative contract | yes | `CONTRACTS.md` | golden and property tests |
| cross-repository dependency | no | local cipher implementation | isolated build |

## implementation checkpoint

the local software slice is implemented and verified on 2026-09-18. the spec
remains accepted rather than retired because physical fold proof and the
authorized github pages publication/live-site smoke remain outside the local
checkpoint.
